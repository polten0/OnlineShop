@using OnlineShop_4M_Models
@model List<Company>

<h1>Компании</h1>

<p>
    <a class="btn btn-primary" asp-controller="Company" asp-action="Create">Создать компанию</a>
</p>

@if (Model.Count > 0)
{
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Название компании</th>
                <th>Отображение</th>
                <th>Управление</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.Country</td>
                    <td>
                        <a asp-controller="Company" asp-action="Edit" asp-route-Id="@item.Id" class="btn btn-primary">
                            Изменить
                        </a>
                        <a asp-controller="Company" asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger">
                            Удалить
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>
        Компании отсутствуют!
    </p>
}